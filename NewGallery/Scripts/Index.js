function login() {
    const username = document.getElementById("Index_login_manual_username").value;
    const password = document.getElementById("Index_login_manual_password").value;

    // request the database from auth.
    // via http request for loaclhost.
    // via distinct api from db connection ( optional ); aim yesh gisha me JS le DB yashar.

    // -> whe got the respnse now we nedd to present proper message/direction to user .
}